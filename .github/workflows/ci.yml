name: Payment CI
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
jobs:
  ci_cd_common:
    uses: ./reusable-github-actions/.github/workflows/ci.yml@main
    with:
      component: payment
      appType: python